site_name: Teste
nav:
  - Home: index.md

# A saída gerada será na pasta docs na raiz do projeto
docs_dir: ../docs  # Isso direciona a saída para a pasta docs na raiz
site_dir: ../site

theme:
  name: "material"

plugins:
  - mkdocstrings:
      default: python